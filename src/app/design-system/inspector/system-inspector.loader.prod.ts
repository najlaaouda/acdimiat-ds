/* =============================================================================
   بديل الإنتاج لمحمّل فاحص النظام.
   -----------------------------------------------------------------------------
   يُستبدَل بـ`system-inspector.loader.ts` عبر `fileReplacements` في تهيئة
   الإنتاج داخل `angular.json`.

   ⚠️ الاستبدال ليس تجميلًا. `isDevMode()` فحصٌ وقت تشغيل، فـwebpack يرى
      `import('react')` في المسار الميّت ويُصدِر القطعة على أي حال: لا تصل
      المتصفّح، لكنها تُكتب في `dist/` وتُنشَر مع البناء. هذا الملفّ لا يذكر
      React إطلاقًا، فلا تُصدَر قطعة أصلًا.

   ⛔ لا تجعله يستورد النسخة الأخرى «لإعادة الاستخدام» — ذلك يعيد المرجع
      ويعيد القطعة معه.
   ============================================================================ */

export async function mountSystemInspector(_host: HTMLElement): Promise<() => void> {
  return () => {};
}
