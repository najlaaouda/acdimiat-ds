import { Directive } from '@angular/core';

/* ============================================================================
   Acadimiat UI — صفّ الخيار داخل اللوحة المنسدلة
   ----------------------------------------------------------------------------
   موجّه بلا سلوك: وجوده كلّه أن أنماط `ui/select/_menu.scss` تعيش في ورقة
   `<ap-menu>`، ولا تصل إلى محتوًى مُسقَط إلّا بمحدّد يسمّيه.

   ⚠️ ولا `role` هنا كذلك: الصفّ يكون `option` داخل `listbox`، و`menuitem`
      داخل `menu`، ورابطًا في قائمة تنقّل. يكتبه المستهلك على العنصر نفسه.

   ⚠️ والحالات تُعلَن بسمات بيانات لا بأصناف: `data-active` للتمييز بلوحة
      المفاتيح، و`aria-selected` للاختيار الفعلي، و`aria-disabled` للمعطَّل —
      وكلّها مقروءة أصلًا لقارئ الشاشة، فلا صنف يكرّرها.
   ============================================================================ */

@Directive({
  selector: '[apMenuOption]',
  standalone: true,
})
export class ApMenuOptionDirective {}
